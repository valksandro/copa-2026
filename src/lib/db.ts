// Stub para build estático (GitHub Pages).
// O Prisma não funciona em static export, mas este arquivo é mantido
// para não quebrar imports. Em site estático, não usamos banco de dados.
export const db = null as unknown
